import { ControldefaultsDirective } from './controldefaults.directive';

describe('ControldefaultsDirective', () => {
  it('should create an instance', () => {
    const directive = new ControldefaultsDirective();
    expect(directive).toBeTruthy();
  });
});
