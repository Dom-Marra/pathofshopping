import { EqualflexDirective } from './equalflex.directive';

describe('EqualflexDirective', () => {
  it('should create an instance', () => {
    const directive = new EqualflexDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
